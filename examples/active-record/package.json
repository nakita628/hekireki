{
  "name": "@hekireki/example-active-record",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf app config/locales/models dev.db dev.db-journal",
    "generate": "prisma validate && prisma generate",
    "db": "prisma db push --url file:./dev.db",
    "bundle": "bundle config set --local path vendor/bundle && bundle install --quiet",
    "check": "bundle exec ruby check.rb",
    "rubocop": "bundle exec rubocop app/models",
    "demo": "pnpm run clean && pnpm run generate && pnpm run db && pnpm run bundle && pnpm run check && pnpm run rubocop"
  },
  "devDependencies": {
    "hekireki": "workspace:*",
    "prisma": "8.1.0-dev.7"
  }
}
