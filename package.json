{
  "name": "hekireki-monorepo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deps": "mkdir -p /tmp/pnpm-store && pnpm config set store-dir /tmp/pnpm-store &&rm -rf node_modules && rm -rf pnpm-lock.yaml && rm -rf ~/.pnpm-store && pnpm store prune && pnpm install --no-prefer-frozen-lockfile",
    "check": "vp check --fix",
    "test": "vp test --project unit",
    "coverage": "vp test --project unit --coverage",
    "lang": "vp run hekireki#build && vp test --project lang",
    "example": "vp run hekireki#build && node example/generate.mjs"
  },
  "dependencies": {
    "hekireki": "link:packages/hekireki"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "vite-plus": "^0.2.6"
  },
  "packageManager": "pnpm@11.22.0"
}
