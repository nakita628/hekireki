[mypy]
strict = True
# Pydantic v2 ships inline type information (py.typed), so the generated
# BaseModel declarations are type-checked against the real pydantic API
# without the pydantic mypy plugin.
plugins =
