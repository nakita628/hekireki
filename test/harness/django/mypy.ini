[mypy]
strict = True
# django-stubs ships the type stubs and a mypy plugin that resolves model
# fields, managers and reverse relations, so the generated models.py is
# type-checked against the real Django ORM API.
plugins = mypy_django_plugin.main

[mypy.plugins.django-stubs]
django_settings_module = settings
