generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "sqlite"
}
generator HekirekiZod {
  provider = "hekireki-zod"
  output   = "zod"
}
model User {
  /// @z.uuid()
  id String @id @default(uuid())
}
